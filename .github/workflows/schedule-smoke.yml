name: Schedule Smoke Test

on:
  schedule:
    - cron: '4-59/5 * * * *'

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Print trigger context
        run: |
          echo "event=${{ github.event_name }}"
          date -u
